#List of parts the name pattern

saved.name.pattern <- list("campus" = "c3", "period" = "1419", "extension"  = ".pdf")

graphics.size.pattern  <- list( "width" = 17.1, "height" = 9, "units" = "cm")
